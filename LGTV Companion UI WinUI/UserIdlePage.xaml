<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="LGTVCompanionUI.UserIdlePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LGTVCompanionUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Padding="20">
        <StackPanel Spacing="12">
            <TextBlock Text="User Idle Mode configuration" Style="{StaticResource SubtitleTextBlockStyle}"/>

            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBlock Text="Idle timeout (minutes):" VerticalAlignment="Center"/>
                <NumberBox x:Name="IdleTimeoutBox" Minimum="1" Maximum="240"
                           SpinButtonPlacementMode="Inline" Width="120"/>
            </StackPanel>

            <CheckBox x:Name="MuteSpeakersCheck" Content="Mute speakers while screen is blanked"/>

            <CheckBox x:Name="DisableFullscreenCheck" Content="Disable idle mode while a fullscreen application is running"/>

            <CheckBox x:Name="VideoWakeLockCheck" Content="Disable idle mode when video wake lock is active"/>
            <CheckBox x:Name="VideoWakeLockFgCheck" Content="Only when application is in foreground" Margin="28,0,0,0"/>
            <CheckBox x:Name="VideoWakeLockFsCheck" Content="Only when application is fullscreen" Margin="28,0,0,0"/>

            <!-- Process control -->
            <TextBlock Text="Process control" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,12,0,0"/>
            <CheckBox x:Name="ProcessControlCheck" Content="Enable detailed process control"/>

            <ListView x:Name="ProcessListView" Height="150" SelectionMode="Single">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <StackPanel Orientation="Horizontal" Spacing="8">
                <HyperlinkButton Content="Add" x:Name="AddProcessLink"/>
                <HyperlinkButton Content="Edit" x:Name="EditProcessLink"/>
                <HyperlinkButton Content="Delete" x:Name="DeleteProcessLink"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
